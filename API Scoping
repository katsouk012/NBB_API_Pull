import requests
import json
import pandas

API_Key = <<PRIVATE>>
url = 'https://connectapi.brandbank.com/api/' + API_Key
output = requests.get(url).json()
i = 0

for page in range(1,1):
    url2 = url + '.json' + '?page=%s'%page
    response = requests.get(url)
    status = response.status_code
    if status == 204:
        break
    else :
        data = requests.get(url).json()
        output = output + data
        #i = i + 1
        #print(len(str(output)))

#print(i)

output_normalized=pd.json_normalize(output, max_level=1)
output_normalized.to_csv("bb_pull.csv")
